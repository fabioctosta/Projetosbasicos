num = int(input("Digite um numero inteiro: "))
print(f" O numero digitado foi: {num}")

resp = str()

a = num
while a != 1:
    resp = resp + str(a % 2)
    a = int(a/2)

if a % 2 == 1:
    resp = resp + str(1)

print(f"Transformando {num} para base binária encontramos: {resp[::-1]}")
